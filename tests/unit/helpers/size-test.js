import { size } from 'dezik/helpers/size';
import { module, test } from 'qunit';

module('Unit | Helper | size');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = size([42]);
  assert.ok(result);
});
